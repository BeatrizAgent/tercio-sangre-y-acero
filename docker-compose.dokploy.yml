services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dokploy
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      DATABASE_URL: ${DATABASE_URL}
    ports:
      - "3000"
