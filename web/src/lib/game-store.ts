// @deprecated import from `@/lib/stores/game-store` instead. This file is
// a thin re-export kept so existing imports of `@/lib/game-store` keep
// working during the migration to the stores/ subfolder.

export * from "./stores/game-store";
