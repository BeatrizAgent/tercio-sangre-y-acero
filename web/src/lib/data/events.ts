// Mission event definitions (the random mid-mission choices).

import { events } from "../../../data/seed-events";
import type { GameEvent } from "../types";

export const eventDefinitions = events as readonly GameEvent[];
