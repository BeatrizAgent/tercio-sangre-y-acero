// @deprecated import from `@/lib/data` (or a specific submodule under
// `@/lib/data/*`) instead. This file is kept as a thin re-export so existing
// imports keep working until they are migrated to the new data layer.

export * from "./data";
export { getAssetPublicPath } from "./data/assets";
